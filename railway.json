{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn bitcoinvsstocks.wsgi:application --bind [::]:${PORT:-8000}"
  }
}
